{
  "name": "agent-managers",
  "private": true,
  "description": "Marp deck: hub-and-spoke vs flat multi-agent workflows",
  "scripts": {
    "slides:build": "make slides",
    "slides:pdf": "make pdf",
    "slides:watch": "make graphs && marp --html --watch --preview slides/slides.md"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "4.5.1"
  }
}
